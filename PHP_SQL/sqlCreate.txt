CREATE TABLE PowerLogger ( 
    ID INT AUTO_INCREMENT , 
    IP VARCHAR(20) NOT NULL, 
    StationNo INT NOT NULL, 
    TimePosted DATETIME NOT NULL, 
    Consumption DOUBLE NOT NULL, 
    PRIMARY KEY (ID) 
);
